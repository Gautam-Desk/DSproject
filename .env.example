# Server Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production
DEBUG=False

# Security Settings
ALLOWED_ORIGINS=*
RATE_LIMIT_PER_MINUTE=120
MAX_CONTENT_LENGTH_BYTES=1048576

# Model Settings
DEFAULT_MODEL_PATH=models/best_fake_news_model.keras
TOKENIZER_CONFIG_PATH=models/tokenizer_config.json
METRICS_PATH=models/metrics.json

# Optional Public Sharing (e.g. ngrok authtoken if using ngrok tunnel)
NGROK_AUTHTOKEN=
